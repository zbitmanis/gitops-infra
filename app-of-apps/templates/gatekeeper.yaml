{{- if .Values.spec.gitops.applications.gitOpsGatekeeper.isEnabled | default false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gatekeeper
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: {{ .Values.spec.gitops.applications.gitOpsGatekeeper.namespace | default 'gatekeeper-system' }}
  project:  {{ .Values.spec.gitops.applications.gitOpsGatekeeper.project | default 'default' }}
  source:
    path: {{ .Values.spec.gitops.applications.gitOpsGatekeeper.appPath }}
    repoURL: {{ .Values.spec.gitops.applications.gitOpsGatekeeper.repoURL | default .Values.spec.default.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
    helm:
      valueFiles:
          {{- range $val := .Values.spec.default.helm.valueFiles }}
            - {{ $val }}
          {{- end}}
{{- if .Values.spec.gitops.applications.gitOpsGatekeeper.isEnabedAutoSync | default false }}
  syncPolicy:
    automated: {}
    retry:
      limit: {{ .Values.spec.gitops.sync.limit | default 5 }}
      backoff:
        duration: {{ .Values.spec.gitops.sync.backoff.duration | default 5s }}
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
